#include <stdio.h>

#define MAX_LEN 10000

int main()
{
    char dir;
    int step;
    while (scanf("%c",&dir))
    {
        switch (dir)
        {
        case 'W':
            scanf("%d",&step);
            break;

        default:
            break;
        }
    }

    scanf("%s",&str);
    return 0;
}