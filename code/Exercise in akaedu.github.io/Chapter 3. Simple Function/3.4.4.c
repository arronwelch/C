#include <stdio.h>
#include <math.h>

double pi = 3.14 + 0.0016;
//double pi = acos(-1.0); // error: initializer element is not a compile-time constant

int minute = 360;
//int hour = minute / 60; // error: initializer element is not a compile-time constant

int main()
{
	return 0;
}
